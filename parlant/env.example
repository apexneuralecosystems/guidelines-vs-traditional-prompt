# Parlant Agent Server Environment Variables
# Copy this file to .env and update values
# 
# IMPORTANT: Never commit the .env file to version control!
#
# Quick Setup:
#   1. Copy this file: cp env.example .env
#   2. Update OPENAI_API_KEY with your actual API key
#   3. Update PARLANT_BASE_URL if needed (default: http://127.0.0.1:8800)
#   4. Restart the server after making changes

# =============================================================================
# OpenAI/OpenRouter Configuration (REQUIRED for Parlant)
# =============================================================================
# Parlant uses OpenAI SDK internally for the agent
# You can use either OpenAI directly or OpenRouter (which provides access to multiple models)
#
# Option 1: Use OpenRouter (recommended - access to multiple models)
#   OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here
#   OPENAI_BASE_URL=https://openrouter.ai/api/v1 (optional, defaults to this if OPENROUTER_API_KEY is set)
#   Get your API key from: https://openrouter.ai/keys
#   See available models: https://openrouter.ai/models
#
# Option 2: Use OpenAI directly
#   OPENAI_API_KEY=sk-your-openai-api-key-here
#   OPENAI_BASE_URL= (leave empty or omit - uses default OpenAI endpoint)
#   Get your API key from: https://platform.openai.com/api-keys
#
# Note: 
#   - If OPENROUTER_API_KEY is set, it will automatically use OpenRouter
#   - If only OPENAI_API_KEY is set, it will use OpenAI directly (or custom endpoint if OPENAI_BASE_URL is set)
#   - The OpenAI SDK respects OPENAI_BASE_URL environment variable

# Use OpenRouter (recommended)
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here
# OPENAI_BASE_URL will default to https://openrouter.ai/api/v1 if OPENROUTER_API_KEY is set

# OR use OpenAI directly (alternative)
# OPENAI_API_KEY=sk-your-openai-api-key-here
# OPENAI_BASE_URL= (leave empty for default OpenAI endpoint, or set custom endpoint)

# =============================================================================
# Parlant Server Configuration
# =============================================================================
# Base URL for the Parlant agent server
# This is used by parlant_client_utils.py when creating client connections
# 
# Development:
#   - Same server: http://127.0.0.1:8800 or http://localhost:8800
# 
# Production Options:
#   - Same server: http://127.0.0.1:8800 (recommended - internal only)
#   - Different server: http://internal-server-ip:8800 (internal network)
#   - Public subdomain: https://parlant.yourdomain.com (if needed)
# 
# Note: 
#   - The Parlant server itself runs on port 8800 by default
#   - This URL is used by clients (like the FastAPI backend) to connect to the server
#   - Parlant server is typically internal-only and not exposed publicly
PARLANT_BASE_URL=http://127.0.0.1:8800

