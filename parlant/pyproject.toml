[project]
name = "parlant-agent"
version = "0.1.0"
description = "Parlant Agent Server for Life Insurance Advisor"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "parlant>=3.0.2",
    "parlant-client>=3.0.1",
    "python-dotenv>=1.0.1",
]

