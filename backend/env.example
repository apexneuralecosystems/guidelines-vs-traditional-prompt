# =============================================================================
# Backend Environment Variables
# =============================================================================
# Copy this file to .env and update values
# 
# IMPORTANT: Never commit the .env file to version control!
# This file contains sensitive information like API keys.
#
# Quick Setup:
#   1. Copy this file: cp env.example .env
#   2. Update OPENROUTER_API_KEY with your actual API key
#   3. For production, update CORS_ORIGINS with your frontend domain(s)
#   4. Restart the server after making changes

# =============================================================================
# OpenRouter API Configuration (REQUIRED)
# =============================================================================
# Get your API key from: https://openrouter.ai/keys
# OpenRouter provides access to multiple AI models through a unified API
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here

# Model to use via OpenRouter (optional, defaults to openai/gpt-4)
# Popular models:
#   - openai/gpt-4 (default) - Best quality, slower
#   - openai/gpt-3.5-turbo - Faster, cheaper, good quality
#   - anthropic/claude-3-opus - High quality, excellent reasoning
#   - anthropic/claude-3-sonnet - Balanced quality and speed
#   - google/gemini-pro - Alternative option
# See full list: https://openrouter.ai/models
OPENROUTER_MODEL=openai/gpt-4

# OpenRouter Headers (optional but recommended)
# HTTP-Referer: Your project's GitHub repo or website URL
# X-Title: A descriptive title for your application
OPENROUTER_HTTP_REFERER=https://github.com/yourusername/yourproject
OPENROUTER_X_TITLE=Life Insurance Comparison Demo

# =============================================================================
# FastAPI Server Configuration
# =============================================================================
# Port for the FastAPI backend server
# Default: 5000
API_PORT=5000

# Host address
# - 0.0.0.0: Allows external connections (use for production)
# - 127.0.0.1: Localhost only (use for development)
# Default: 0.0.0.0
API_HOST=0.0.0.0

# =============================================================================
# Frontend Configuration
# =============================================================================
# Port for the Next.js frontend server
# Default: 3300
FRONTEND_PORT=3300

# Frontend URL (used for CORS defaults and redirects)
# Development: http://localhost:3300
# Production Examples:
#   - Single domain: https://yourdomain.com
#   - With www: https://www.yourdomain.com
#   - Subdomain: https://app.yourdomain.com
FRONTEND_URL=http://localhost:3300

# =============================================================================
# CORS Configuration (REQUIRED)
# =============================================================================
# CORS Allowed Origins (comma-separated list, no spaces around commas)
# 
# This is REQUIRED - the application will not start without it
# Set this to match your frontend URL(s) exactly
#
# Development Examples:
#   - Frontend on port 3002: CORS_ORIGINS=http://localhost:3002,http://127.0.0.1:3002
#   - Frontend on port 3300: CORS_ORIGINS=http://localhost:3300,http://127.0.0.1:3300
#   - Frontend on port 3000: CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
#
# Production Examples:
#   - Single domain: CORS_ORIGINS=https://yourdomain.com
#   - Multiple domains: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
#   - Multiple apps: CORS_ORIGINS=https://app.example.com,https://admin.example.com
#   - With subdomains: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://app.yourdomain.com
#
# IMPORTANT: 
#   - Include all variants you might use (localhost, 127.0.0.1, different ports)
#   - For production, use HTTPS URLs only
#   - No wildcards or defaults - must be explicitly set
#
# Development (update with your actual frontend port):
CORS_ORIGINS=http://localhost:3002,http://127.0.0.1:3002

# Production (update with your actual domains):
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Optional: CORS Regex Pattern (for additional flexibility if needed)
# Use with caution - only if you need dynamic origin matching
# Example: CORS_ORIGINS_REGEX=https://.*\.yourdomain\.com
# CORS_ORIGINS_REGEX=

# =============================================================================
# Parlant Server Configuration (REQUIRED)
# =============================================================================
# Base URL for the Parlant agent server
# This is used by the FastAPI backend to connect to the Parlant agent server
# 
# The Parlant server runs separately in the parlant/ directory
# Make sure parlant/parlant_agent_server.py is running before starting the backend
# 
# Development:
#   - Same server: http://127.0.0.1:8800 or http://localhost:8800
# 
# Production Options:
#   - Same server: http://127.0.0.1:8800 (recommended - internal only)
#   - Different server: http://internal-server-ip:8800 (internal network)
#   - Public subdomain: https://parlant.yourdomain.com (if needed)
# 
# Note: 
#   - Parlant server is typically internal-only and not exposed publicly
#   - This must match the URL where the Parlant server is accessible
#   - This is REQUIRED - the application will not start without it
PARLANT_BASE_URL=http://127.0.0.1:8800

# =============================================================================
# Demo Queries (Optional)
# =============================================================================
# Custom demo queries in JSON array format
# If not set, default queries from config.py will be used
#
# Format: JSON array of strings
# Example:
# DEMO_QUERIES=["Query 1", "Query 2", "Query 3"]
#
# Full example with actual queries:
# DEMO_QUERIES=["I want to replace my existing $500k term policy with a whole life policy. What should I do?", "I'm 35 years old, make $80,000 a year, and have 2 kids. How much life insurance coverage should I get?"]
#
# Note: Use double quotes for JSON strings, escape internal quotes with \"
# DEMO_QUERIES=

# =============================================================================
# Production Configuration Example
# =============================================================================
# Uncomment and update these for production deployment:
#
# OPENROUTER_API_KEY=sk-or-v1-your-production-api-key
# OPENROUTER_MODEL=openai/gpt-4
# API_PORT=5000
# API_HOST=0.0.0.0
# FRONTEND_URL=https://yourdomain.com
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# PARLANT_BASE_URL=http://127.0.0.1:8800  # Internal, same server
#   OR: https://parlant.yourdomain.com     # If on different server/subdomain
